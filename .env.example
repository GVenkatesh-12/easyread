# API base URL
# For Vercel: set VITE_API_URL=/api to avoid CORS (requests are proxied via vercel.json)
# For other hosts: set VITE_API_URL=https://api-ebook.duckdns.org (or your API URL)
VITE_API_URL=/api
